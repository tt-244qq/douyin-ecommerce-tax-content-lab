# 工程目录规范

## 项目目标

本项目用于运行“抖音短视频获客”全链路：

1. 用户给主题。
2. 生成文案或视频。
3. 发布前写盲预测。
4. 发布后登记链接。
5. T+3 回收数据。
6. 复盘表现，更新下一轮内容判断。

当前账号方向：电商垂直领域财税，品牌露出使用 `西安注册公司找峪诚`。

## 目录分工

### 核心链路

- `scripts/`：拍摄前最终稿或待预测脚本。
- `predictions/`：盲预测日志。发布数据回来后不能改 `## 预测` 段，只能追加复盘。
- `videos/`：视频发布后的登记目录。后续每条已拍/已发内容在这里沉淀最终稿、链接、数据和复盘。
- `rubric_notes.md`：评分规则来源。随着复盘数据迭代，但不堆无效历史。
- `rubric-memo.md`：复盘观察和临时判断。
- `audience.md`：受众画像。
- `candidates.md`：候选选题池。
- `script_patterns.md`：脚本结构、爆点模式和对标拆解。

### 作品生成

- `作品生成/生成输入/`：用于生成视频的口播输入。
- `作品生成/素材片段/`：程序生成的视频底片或中间素材。
- `作品生成/视频成片/`：最终可发布或可交付给业务侧确认的视频。
- `作品生成/生成说明.md`：本轮生成链路、规格、时长和结构。
- `作品生成/视频风格与元素库.md`：视频风格、统一元素、可复用视觉容器。
- `作品生成/发布索引方案.md`：标题、封面字、关键词、话题、置顶评论和私信关键词。

### 状态与交接

- `STATUS.md`：当前进度看板。
- `WORKFLOW.md`：标准工作流。
- `HANDOFF.md`：上下文恢复文档，防止会话丢失。
- `.cheat-state.json`：cheat-on-content 状态文件。

### 本地隐私

- `.项目隐私/`：本地隐私资料，不提交 GitHub。
- `.项目隐私/抖音参考账号.md`：本账号、风格参考账号、内容参考账号和使用规则。

## 隐私规则

- `.项目隐私/` 已加入 `.gitignore`。
- 不把个人抖音号、参考账号清单、登录态、Cookie、截图缓存提交到 GitHub。
- 公开文档只能描述“存在本地隐私参考”，不能写出具体账号清单。

## 后续执行规则

### 用户给主题

生成文案或视频前，默认读取：

- `.项目隐私/抖音参考账号.md`
- `script_patterns.md`
- `rubric_notes.md`
- `audience.md`
- `作品生成/视频风格与元素库.md`

然后输出：

- 文案稿：进入 `scripts/`
- 视频输入：进入 `作品生成/生成输入/`
- 成片：进入 `作品生成/视频成片/`
- 发布索引：更新 `作品生成/发布索引方案.md`

### 用户给发布链接

收到财务反馈的网址或抖音发布链接后：

1. 登记发布信息。
2. 不修改已有预测段。
3. 等 T+3 数据回收。
4. 将播放、点赞、评论、收藏、转发、私信、加微信等数据写入复盘。
5. 根据复盘更新 `rubric-memo.md`、`audience.md`、`script_patterns.md` 或下一轮选题。

## 当前推荐结构

后续视频默认遵循：

- 全局统一：品牌、系列编号、字幕样式、私信关键词、财税专业感。
- 单条差异：每条必须使用不同内容容器，例如冲突剧场、风险侦查、决策选择器、案例拆解、清单自查、老板问答。
- 风格参考：本地隐私文件中的风格参考账号。
- 内容参考：本地隐私文件中的内容参考账号。
