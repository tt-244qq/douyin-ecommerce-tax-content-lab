# 项目提示词

本文件是后续 agent 进入项目后的总提示词。它的优先级高于普通说明文档，用来保护 `cheat-on-content` 闭环、历史数据和隐私资料。

## 目标

最终目标是让本项目达到 `cheat-on-content` skill 的预期：

```
脚本 -> 盲预测 -> 拍摄/生成 -> 发布登记 -> T+3 复盘 -> rubric 校准 -> 下一轮内容改进
```

所有动作都服务这个闭环，不做脱离预测和复盘的内容生产。

## STATUS.md 强制更新

`STATUS.md` 是项目状态看板，任何会改变项目进度的动作都必须同步更新，不能只更新 `.cheat-state.json` 或口头汇报。

必须更新 `STATUS.md` 的场景：

- 发布登记、补 URL、补平台 ID、补发布时间。
- 新增或修改 `pending_retros`、`in_progress_session`、`last_published_*`、`calibration_samples` 等状态字段。
- 新增脚本、预测、拍摄记录、视频目录、成片或发布索引。
- 完成复盘、写入实绩、更新 rubric-memo / rubric_notes / audience。
- 更新候选池、生成推荐选题、抓热点或改变下一步优先级。
- 修改抖音登录态、浏览器打开方式、未登录暂停规则等操作性流程。

收尾规则：

1. 每次执行上述动作后，必须打开并更新 `STATUS.md` 的“最近更新”“当前状态”“待复盘”“候选池 / 新脚本”“成片”“健康度”“下一步建议”等相关段落。
2. commit / push 前必须运行 `git status --short`，确认 `STATUS.md` 已 staged；如果本轮改变了项目进度但 `STATUS.md` 没有变化，必须先补更新再提交。
3. 如果用户只要求查看状态而不改变任何文件，`STATUS.md` 可以不写；但如果发现它明显过期，要提醒并询问是否同步，或在用户要求“更新状态”时直接更新。
4. 最终回复里必须明确说明 `STATUS.md` 是否已更新。

## Skill 更新策略

当用户说“更新这个 skill”“检查 cheat-on-content 是否更新”“升级 skill”或类似指令时，按以下流程执行：

1. 读取 `/mnt/d/wtf1124/project/cheat-on-content/SKILL.md`。
2. 在 skill 仓库执行 `git fetch --all --prune`。
3. 比较 `HEAD..origin/main` 的变更文件。
4. 如果只变更 README、图片、star-history、CI 等展示文件：
   - 可以 `git pull --ff-only` 更新本地 skill。
   - 不迁移项目，不改历史数据。
   - 在本项目记录“无 schema / 子 skill / 模板变化”。
5. 如果变更包含以下任一类文件：
   - `SKILL.md`
   - `skills/**/SKILL.md`
   - `migrations/**`
   - `shared-references/**`
   - `templates/**`
   - `starter-rubrics/**`
   - `hooks/**`
   则必须继续检查是否影响本项目。
6. 读取 `migrations/registry.md`，比较 `LATEST_SCHEMA` 和本项目 `.cheat-state.json` 的 `schema_version`。
7. 如果 schema 落后，按 `skills/cheat-migrate/SKILL.md` 和对应 migration 文件顺序迁移。
8. 迁移前必须备份 `.cheat-state.json`，迁移失败时不推进 `schema_version`。
9. 所有更新必须增量执行，不能重跑 init 覆盖项目。
10. 未经用户确认，不 commit，不 push。

## 预测隔离评分策略

后续用户说“启动预测”“给这稿子预测”“今天脚本跑预测”等类似指令时，必须优先使用隔离子代理做盲评分。

执行规则：

1. 优先使用 `multi_agent_v1.spawn_agent` 创建隔离评分子代理。
2. 子代理 `fork_context` 必须为 `false`，只给它目标脚本路径和 `rubric_notes.md`。
3. 子代理只允许读取：
   - `rubric_notes.md`
   - 当前要预测的 `scripts/YYYY-MM-DD_<id>_<主题>.md`
4. 子代理禁止读取：
   - `.cheat-state.json`
   - `predictions/`
   - `videos/`
   - `rubric-memo.md`
   - `audience.md`
   - `benchmark.md`
   - `candidates.md`
   - `HANDOFF.md`
   - `STATUS.md`
   - 任何发布数据、复盘数据或历史表现文件
5. 子代理只输出 JSON 评分，不直接写文件。
6. 主会话拿到 JSON 后，再写入 `predictions/`。
7. 如果子代理工具不可用，才允许 fallback 到 `main-claude-self`，并必须在预测文件和 `.cheat-state.json` 标记 `last_prediction_self_scored: true`。
8. 如果发布前补跑隔离盲评，不覆盖原 `## 预测 v1`，只追加“发布前补充盲评”。
9. 已发布后的内容不能补写预测，只能复盘。

## 历史数据保护

这些文件和目录是历史事实，不允许丢失或覆盖：

- `scripts/`
- `predictions/`
- `videos/`
- `candidates.md`
- `benchmark.md`
- `script_patterns.md`
- `audience.md`
- `rubric_notes.md`
- `rubric-memo.md`
- `.cheat-state.json`
- `作品生成/视频成片/`
- `作品生成/素材片段/`

保护规则：

- 不删除已有预测文件。
- 不改写任何 `## 预测 v1` / `## 预测 v2` 段。
- 不把已发布视频重新渲染、替换、调色、改字幕或改风格。
- 已发布视频只做发布登记、数据回收、复盘和经验沉淀。
- 历史视频暴露的问题，只写入 `rubric-memo.md`、`script_patterns.md`、`作品生成/视频风格与元素库.md` 或下一条当天脚本。
- 如果要迁移、整理或归档文件，必须保留原始信息可追溯。

## 隐私资料使用

隐私资料在 `.项目隐私/`，该目录不提交 GitHub。

当前隐私资料包括：

- `.项目隐私/抖音参考账号.md`
- `.项目隐私/对标账号运行记录.md`

使用规则：

- 生成文案、视频、选题、发布索引、复盘前，默认读取 `.项目隐私/抖音参考账号.md`。
- 风格参考账号只用于视频包装、节奏、镜头变化、系列感和整体气质。
- 内容参考账号只用于选题、痛点、财税表达、转化入口和评论区问题。
- 不把具体账号 ID 写入公开文件。
- 公开文件只写“风格参考”“内容参考”的派生规则和低置信观察。
- 当前没有逐条视频样本和数据，benchmark 只能视为账号级低置信参考，不能当作正式校准样本。
- 如果 `.项目隐私/` 内部记录出现账号 ID 不一致，先以 `.项目隐私/抖音参考账号.md` 为当前配置来源；执行账号历史抓取或发布前让用户确认。

## 抖音浏览器登录态

发布登记或复盘需要打开抖音时，优先复用已登录的专用 Chrome Profile，不要反复复制主 Chrome Profile，也不要每次新建一次性目录。

当前已登录态目录：

```text
C:\Users\Administrator\AppData\Local\Temp\wyz-chrome-debug-user-data
```

项目固定目录预留为：

```text
D:\wtf1124\wyz\.browser\douyin-chrome-user-data
```

注意：2026-07-09 已把旧目录复制到项目固定目录，但冒烟检查显示固定目录窗口仍为“未登录”。因此在用户明确进入固定目录窗口登录成功前，默认继续用当前已登录态目录；固定目录不能直接当作已登录目录使用。

启动方式：

```bash
powershell.exe -NoProfile -Command 'Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" -ArgumentList @("--remote-debugging-port=9222","--user-data-dir=C:\Users\Administrator\AppData\Local\Temp\wyz-chrome-debug-user-data","--profile-directory=Default","--no-first-run","https://www.douyin.com/user/self?from_tab_name=main"); Start-Sleep -Seconds 5; (Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:9222/json/version").Content'
```

注意：

- 从 Bash 调 PowerShell 时，命令外层优先用单引号，避免 `$url`、`$_`、`$page` 被 Bash 展开成空值。
- 如果必须写复杂 PowerShell，先写到 `/tmp/*.ps1`，再用 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$(wslpath -w /tmp/file.ps1)"` 执行。
- 如果 `http://127.0.0.1:9222/json/list` 中抖音页标题或页面文本显示“未登录”“登录”，立即暂停，不抓作品、不登记 publish，等用户在这个固定 Profile 的 Chrome 窗口登录。
- 如果任何抖音页面、创作者中心页面或 CDP 读取结果显示未登录，必须立刻暂停当前检查 / 复盘 / 发布登记流程；禁止继续抓取作品、读取数据、写入发布字段或复盘字段，直到用户确认已在这个固定 Profile 的 Chrome 窗口重新登录。
- 用户登录后，不要删除或覆盖当前已登录态目录。后续继续用同一目录启动，登录态才会保留。
- 如果用户愿意做一次长期化迁移：用项目固定目录启动 Chrome，让用户在该窗口登录抖音；确认 CDP 不再显示“未登录”后，再把默认启动命令切换到 `D:\wtf1124\wyz\.browser\douyin-chrome-user-data`。

## 视频生成规则

- 每天默认产出 3 条脚本和 3 条视频；除非用户明确说只做 1 条或指定数量，不要只生成单条。
- 同一天 3 条要形成一组：选题有关联、首页视觉统一，但三条的主视觉容器必须不同。
- `作品生成/视频风格与元素库.md` 是公共模板，所有视频生成必须遵守。
- 每条视频的自定义风格写在当天 `scripts/YYYY-MM-DD_<id>_<主题>.md` 的 `## 视频设计`。
- 新视频生成默认读取当天 `scripts/` 目标脚本，不默认读取 `作品生成/生成输入/`。
- `作品生成/归档/生成输入/` 只保存历史输入，除非复刻旧视频，否则不读取。
- 后续所有视频调整，只针对当天正在制作的视频。
- 最终成片使用最高可用帧率，优先 `60fps`。
- 新视频不要和最近已生成视频使用同类型主视觉容器；必须有局部风格差异，但首页整体保留统一品牌条、深色专业底、白色信息卡、风险色、底部 CTA 和字幕安全区。
- 可以适当加入低音量背景音乐，但音乐只做氛围底，不得有人声或强鼓点，不得覆盖讲解声音；口播必须始终是主声道。
- 至少三天内不得复用相同视频风格矩阵；同一批 3 条也必须分别使用不同主视觉容器、镜头逻辑和动效节奏。
- 讲解语音要真人化、自然、有呼吸和停顿，禁止明显 AI 播报腔；优先使用真人录音或高自然度真人化音色。
- 可加入动画讲解和真人渲染讲解员。默认使用用户指定的 2025年03月17日 11:16 发布视频中的人物作为主讲解员参考；如果当前项目没有该人物素材，先使用“真人渲染讲解员占位”，并在生成说明中标记素材待替换。

## 当前目录调用关系

- `scripts/`：拍摄/生成前最终脚本，也是视频生成默认输入。
- `predictions/`：发布前盲预测，预测段不可改。
- `videos/`：已拍/已发后沉淀最终拍摄稿和复盘报告。
- `作品生成/`：视频生成模板、提示词、素材、成片和发布索引。
- `benchmark.md`：公开对标学习记录，不含账号 ID。
- `.项目隐私/`：真实账号 ID 和隐私运行记录。
- `script_patterns.md`：写稿模式和对标拆解。
- `rubric_notes.md`：blind-safe 评分规则。
- `rubric-memo.md`：含实绩/复盘证据，blind 子代理禁止读取。

## 当前已知状态

- `.cheat-state.json` 当前 schema 是 `1.4`。
- 当前 `cheat-on-content` 最新 schema 也是 `1.4`。
- 当前账号仍处于 cold-start，复盘样本为 0。
- 已发布的 2026-07-06 三条视频已登记为待复盘。
- 2026-07-07 三条脚本已启动预测，但因为当前 Codex 会话没有隔离 Task 子代理，预测标记为 `main-claude-self` 自评分。
