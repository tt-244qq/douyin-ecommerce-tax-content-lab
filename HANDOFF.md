# 项目交接记忆

更新时间：2026-07-08 00:00 +08:00

## 当前目标

西安注册公司找峪诚使用企业认证抖音账号做电商垂直领域财税获客短视频。

本项目已接入 `cheat-on-content` 标准链路，用于把内容生产变成：

脚本 -> blind score -> 发布前预测 -> 拍摄 -> 发布登记 -> T+3 复盘 -> rubric 校准。

## 后续 Agent 必读规则

以下规则优先级很高，用于防止后续视频生成时误读旧流程：

0. 先读 `项目提示词.md`，再读本文件。
1. `作品生成/视频风格与元素库.md` 是公共模板，所有视频生成必须遵守。
2. 公共模板只放全局规范、元素库、字幕、安全区、帧率和验收标准，不放单条视频专属风格。
3. 每条视频的自定义风格只写在当天 `scripts/YYYY-MM-DD_<id>_<主题>.md` 里，主要使用 `## 视频设计` 和 `## 发布承接`。
4. 新视频生成默认读取当天 `scripts/` 目标脚本，不再默认读取 `作品生成/生成输入/`。
5. `作品生成/生成输入/` 已转为历史归档，位置是 `作品生成/归档/生成输入/`，除非复刻旧视频，否则不要读取。
6. 后续所有视频调整，只针对当天正在制作的视频。
7. 已发布的历史视频不要重新生成、替换、调色、改字幕或改风格；历史视频只做发布登记、数据回收和复盘。
8. 如果发现历史视频存在问题，只把经验写入 `rubric-memo.md`、`script_patterns.md`、`作品生成/视频风格与元素库.md` 或下一条当天脚本，不回改已发布成片。
9. 最终成片使用最高可用帧率，优先 `60fps`；性能不足可以低帧率预览，但交付文件必须重新输出最高可用帧率。
10. 未经用户确认，不要 commit 或 push 当前本地改动。
11. 后续启动预测必须优先使用 `multi_agent_v1.spawn_agent` 做隔离盲评；子代理只读 `rubric_notes.md` 和目标脚本，不读预测、复盘、状态、受众画像或历史表现。

## Skill 更新检查记录

### 2026-07-08

- 已检查 `/mnt/d/wtf1124/project/cheat-on-content`。
- 执行 `git fetch --all --prune` 后发现远端有更新。
- 已 `git pull --ff-only` 更新本地 skill 到 `77b903f`。
- 本次远端变化只涉及 README、star-history 图、CI 和图表生成工具：
  - `.github/workflows/star-history.yml`
  - `README.md`
  - `docs/README_CN.md`
  - `docs/star-history.svg`
  - `tools/gen-star-history.py`
  - `tools/xkcd-font.css`
- 未涉及 `SKILL.md`、`skills/**`、`migrations/**`、`shared-references/**`、`templates/**`、`starter-rubrics/**` 或 `hooks/**`。
- `migrations/registry.md` 的 `LATEST_SCHEMA` 仍为 `1.4`。
- 本项目 `.cheat-state.json` 的 `schema_version` 也是 `1.4`。
- 结论：本次 skill 更新不需要迁移项目，不改历史数据。

后续用户说“更新这个 skill”时，按 `项目提示词.md` 的 `Skill 更新策略` 执行。

## 内容形态配置

`cheat-on-content` 配置为：

- `content_form`: `tutorial-builder`
- 含义：教程 / 工具教学 / 专业知识讲解
- 典型时长：30 秒
- 发布频率：日更
- 数据回收：manual
- 当前处于 cold-start，校准样本为 0

选择理由：

该账号不是纯观点号，而是电商财税顾问账号。核心内容是用专业知识帮电商老板识别风险、做自查、产生咨询。可以使用观点视频的冲突感，但主形态应是专业知识讲解。

## 当前标准目录

保留并使用以下 `cheat-on-content` 文件：

- `.cheat-state.json`
- `rubric_notes.md`
- `rubric-memo.md`
- `script_patterns.md`
- `audience.md`
- `WORKFLOW.md`
- `STATUS.md`
- `candidates.md`
- `scripts/`
- `predictions/`
- `videos/`
- `samples/`

旧的临时中文运营目录已经清理。

## 三条脚本

### 1. 无票成本

脚本：

- `scripts/2026-07-06_de75c1e8e74e_无票成本.md`

预测：

- `predictions/2026-07-06_de75c1e8e74e_无票成本.md`

blind score：

- ER 4
- HP 5
- QL 4
- NA 4
- AB 4
- SR 3
- SAT 2
- composite 7.43

判断：

三条中最适合优先发布。强点是“现实利润薄、账面利润高”的认知反差，适合引出成本票、证据链、无票成本处理咨询。

私信关键词：

- 成本

置顶评论：

```text
你店里最大的一类无票成本是什么？进货、达人、物流还是人工？
```

### 2. 平台流水自查

脚本：

- `scripts/2026-07-06_c94c64246130_平台流水自查.md`

预测：

- `predictions/2026-07-06_c94c64246130_平台流水自查.md`

blind score：

- ER 2
- HP 5
- QL 4
- NA 4
- AB 4
- SR 3
- SAT 2
- composite 6.86

判断：

获客型专业内容。开头风险明确，适合引导电商老板做流水、收款账户、开票金额三项自查。短板是情绪强度和人格感偏弱。

私信关键词：

- 电商自查

置顶评论：

```text
电商老板可以先查 3 个数：平台流水、对公入账、已开票金额。
```

### 3. 主体选择

脚本：

- `scripts/2026-07-06_f9ed4c45e01c_主体选择.md`

预测：

- `predictions/2026-07-06_f9ed4c45e01c_主体选择.md`

blind score：

- ER 3
- HP 4
- QL 3
- NA 4
- AB 4
- SR 2
- SAT 2
- composite 6.29

判断：

适合作为新手电商老板入口内容。问题明确，但用户意向阶段偏早，咨询质量可能弱于前两条。

私信关键词：

- 主体

置顶评论：

```text
你现在是个体户、个人店，还是公司主体？
```

## 推荐发布顺序

1. 无票成本
2. 平台流水自查
3. 主体选择

原因：

- `无票成本` 的认知反差最强，最容易触发电商老板的当下焦虑。
- `平台流水自查` 适合做风险自查，承接私信不错。
- `主体选择` 更偏早期用户教育，转化质量可能较低。

## 当前作品生成状态

运维侧本轮工作已结束。

已生成并推送新版差异化风格样片：

- `作品生成/视频成片/无票成本.mp4`
- `作品生成/视频成片/平台流水自查.mp4`
- `作品生成/视频成片/主体选择.mp4`

成片规格：

- 画幅：竖屏 `9:16`
- 分辨率：`1080x1920`
- 帧率：后续按公共模板优先输出最高可用帧率，优先 `60fps`
- 品牌露出：`西安注册公司找峪诚`
- 不显示私人姓名

三条样片的风格容器：

- `无票成本`：冲突剧场，私信关键词 `成本`
- `平台流水自查`：风险侦查，私信关键词 `电商自查`
- `主体选择`：决策选择器，私信关键词 `主体`

旧版废视频和旧版 MoneyPrinterTurbo 任务缓存已清理。

作品生成相关文档：

- `作品生成/生成说明.md`
- `作品生成/视频风格与元素库.md`
- `作品生成/视频生成提示词.md`
- `作品生成/发布索引方案.md`
- `作品生成/归档/生成输入/*_差异化风格口播.md`
- `工程目录规范.md`

本地隐私参考：

- `.项目隐私/抖音参考账号.md`
- 该文件不提交 GitHub，后续生成文案、生成视频、分析发布反馈前默认读取。
- 公开文档只记录存在本地隐私参考，不写具体账号。

最新提交：

- 以 `git log -1` 为准；本轮提交主题为 `生成差异化短视频与发布索引`

## 下一步业务侧会做什么

业务侧确认是否直接发布三条样片，或用真人口播替换 AI 配音后再发布。发布后把抖音链接贴回来。

收到链接后要继续 `cheat-on-content` 链路：

1. 对每条执行 `拍了 <script-path>` 的等价流程：
   - 创建 `videos/<date>_<id>_<short>/`
   - 复制最终拍摄稿到 `videos/<...>/script.md`
   - 如用户说明拍摄稿有大改，要比较是否需要 v2 预测
2. 对每条执行 `已发布 <url>` 的等价流程：
   - 更新对应 `predictions/*.md` 的发布信息
   - 更新 `.cheat-state.json` 的 `pending_retros`
   - 标记 T+3 复盘
3. 尝试自动读取抖音公开链接信息：
   - 标题 / 文案
   - 作者
   - 发布时间
   - 可见互动数
   - 可见评论
4. 如果抖音限制访问，则让用户提供最少截图或文字。

## 用户明天贴链接时的最小格式

推荐用户这样发：

```text
无票成本：https://...
平台流水自查：https://...
主体选择：https://...
```

如果有后台截图，补充：

```text
每条视频的播放、点赞、评论、收藏、转发、私信数、加微信数。
```

## 抖音链接采集能力边界

可以尝试自动采集公开视频可见信息：

- 视频标题 / 文案
- 作者昵称 / 账号信息
- 发布时间
- 点赞、评论、收藏、转发等公开数值
- 部分公开评论
- 视频主题判断

不能保证自动采集：

- 完播率
- 5 秒留存
- 平均播放时长
- 粉丝画像
- 私信数
- 加微信数
- 企业号后台线索
- 登录、验证码、风控挡住的信息

如果自动抓不到，要求用户补最少截图，不要让用户填复杂表。

## 重要纪律

三条内容都已写入发布前 blind prediction。后续不能再改 `predictions/*.md` 的 `## 预测 v1` 段。

发布后数据只能写到：

- `predictions/*.md` 的 `## 复盘` 段
- `videos/<...>/report.md`
- `.cheat-state.json` 的状态字段

不要把发布后的数据写入 `rubric_notes.md`。

`rubric_notes.md` 已处理为 blind-safe，只保留评分公式和维度定义。
